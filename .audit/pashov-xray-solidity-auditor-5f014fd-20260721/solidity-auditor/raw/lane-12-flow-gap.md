[Feynman: GovStaker.migrateStake] A permanently locked staker moves all of their stake from the old staking contract into whatever staking contract the registry now names, then asks the new contract to record that the account must remain permanently locked.
[Socratic: src/dao/staking/GovStaker.sol:420 — why?] Why is the migration considered complete after calling a hook whose in-scope implementation does nothing and whose postcondition is never checked?
[Inversion: GovStaker.migrateStake] (1) Migrate a permanent account into another unmodified GovStaker and inspect its default-false permanent flag; (2) wait one epoch so pending stake realizes, then call cooldown; (3) use a PermaStaker wrapper's arbitrary execute path to call cooldown/unstake on the migrated stake.

[Feynman: GovStaker.onPermaStakeMigrate] This is the receiving contract's chance to mark a migrated account as permanently locked, but this implementation accepts the notification and changes nothing.
[Inversion: GovStaker.onPermaStakeMigrate] (1) Call it for an arbitrary account and observe no state change; (2) migrate real stake and observe the recipient account remains ordinary; (3) immediately compare isPermaStaker on old and new contracts to show the promised property disappears at the boundary.

[Feynman: LiquidationHandler.migrateCollateral] A retired liquidation handler will hand its remaining vault shares and matching bad-debt record only to the replacement handler, after the registry already points at that replacement.
[Socratic: src/protocol/LiquidationHandler.sol:57 — why?] What callable function on the replacement handler makes it become msg.sender at the retired handler and books the returned debt?
[Inversion: LiquidationHandler.migrateCollateral] (1) Have governance call the retired handler and hit the new-handler-only sender check; (2) call every public entry on the replacement handler and find none that invokes the retired handler; (3) switch the registry first, then show the old handler cannot process collateral while no authorized caller can migrate it.

[Feynman: RetentionReceiver.claimEmissions] Once per epoch, this function collects this receiver's emissions plus a fixed treasury contribution, forwards as much as remains under the lifetime cap to retention rewards, and returns excess to treasury.
[Socratic: src/dao/emissions/receivers/RetentionReceiver.sol:51 — why?] Why does zero mean both "never claimed" and the real first epoch even though the function stores the current epoch back into that same field?
[Inversion: RetentionReceiver.claimEmissions] (1) Call twice while getEpoch is zero; (2) use treasuryAllocationPerEpoch greater than zero and observe both calls pull a full contribution; (3) repeat until distributedRewards reaches MAX_REWARDS, defeating the intended once-per-epoch pacing.

[Feynman: RetentionIncentives._updateReward] The contract first pays an account as though its last recorded insurance balance remained valid for the whole elapsed period, and only afterward asks the InsurancePool whether that balance has fallen and reduces future weight.
[Socratic: src/dao/RetentionIncentives.sol:153 — why?] Why is stale weight used to settle the entire interval before the external source of truth is consulted, when that source may have fallen immediately after the prior checkpoint?
[Inversion: RetentionIncentives._updateReward] (1) Checkpoint with 1,000 shares, withdraw all shares one second later, and wait a week before claiming; (2) avoid every optional checkpoint so the stale 1,000 weight survives; (3) claim after a full reward cycle so `earned` credits the whole interval before the function finally observes zero InsurancePool shares.

[Feynman: InsurancePool.redeem] After an account has waited through its exit delay, this function clears the withdrawal queue, burns the requested pool shares, and sends the matching reUSD to the chosen receiver; it never tells the separate retention program when the account's insurance balance fell.
[Inversion: InsurancePool.redeem] (1) Redeem immediately after a retention checkpoint; (2) redeem all shares so the external source of truth becomes zero; (3) leave the separate retention record untouched until the end of the reward period.

FINDING | contract: RetentionIncentives | function: _updateReward | bug_class: stale-external-balance-accrual | group_key: RetentionIncentives | _updateReward | stale-external-balance-accrual
path: eligible InsurancePool depositor → RetentionIncentives checkpoint records positive weight → depositor exits and redeems/burns all InsurancePool shares → no retention hook runs → a reward period elapses → depositor calls getReward → `_updateReward` credits the entire elapsed period at the stale weight before reading the now-zero InsurancePool balance → depositor receives rewards for capital no longer retained
seam: three-way
trace: `InsurancePool.redeem/withdraw` burns the user's external shares without touching RetentionIncentives → `RetentionIncentives.getReward(account)` enters `_updateReward` → lines 152-159 advance the global integral and call `earned(account)` using `_balances[account]` → only lines 161-169 query `InsurancePool.balanceOf(account)` and reduce the weight after the stale interval has already been credited
violated_principle: Retention rewards are supposed to follow the user's retained InsurancePool balance; capital removed from the insurance reserve must stop earning retention incentives from the removal time.
root_defect: The external balance decrease and retention checkpoint are not coupled, and the lazy checkpoint settles accrued rewards before synchronizing its external source of truth.
proof: Let the attacker be the only weighted account with `_balances[attacker] = _totalSupply = 1_000e18`; queue `100e18` rewards for one seven-day period and checkpoint at `t0`. The attacker redeems all 1,000 InsurancePool shares at `t0+1`, making `InsurancePool.balanceOf(attacker)=0`, but `_balances[attacker]` remains `1_000e18` (the integration test at `test/integration/Retention.t.sol:88-97` explicitly asserts this persistence). At `t0+7 days`, `getReward(attacker)` first computes a `0.1e18` reward-per-token increment and `earned = 1_000e18 * 0.1e18 / 1e18 = 100e18`; only afterward does it observe zero shares and set future weight to zero. The attacker therefore receives the whole cycle despite holding no reserve stake for essentially the entire period. Any permissionless checkpoint intended to stop this can be front-run by the attacker's claim because the claim performs the stale settlement first.
description: A former InsurancePool depositor can keep earning retention rewards indefinitely until checkpointed because reward settlement precedes synchronization of the external balance decrease.
fix: Couple InsurancePool share burns to RetentionIncentives with an atomic before/after checkpoint (or a dedicated balance-change hook) so rewards settle at the old balance and the new weight is installed in the same transaction as the burn.

FINDING | contract: RetentionReceiver | function: claimEmissions | bug_class: epoch-zero-sentinel-replay | group_key: RetentionReceiver | claimEmissions | epoch-zero-sentinel-replay
path: any caller during Core epoch zero → repeatedly calls permissionless `claimEmissions` → every call treats `lastEpoch == 0` as a first claim and sets `epochsSince = 1` → writing `lastEpoch = getEpoch()` writes zero again → each call pulls another full treasury allocation and queues it to retention rewards → lifetime retention cap is exhausted in the first epoch
seam: three-way
trace: execution uses zero as the uninitialized sentinel → the real epoch getter also returns zero → the state write cannot leave the sentinel → each replay crosses the treasury token `transferFrom` and reward-stream `queueNewRewards` boundaries → the program ends with many epochs of budget distributed immediately
violated_principle: `claimEmissions` is documented and implemented as a once-per-epoch distribution, and the capped retention budget is intended to be paced across epochs rather than callable repeatedly in one epoch.
root_defect: `lastEpoch == 0` conflates “never claimed” with the valid first epoch, so the once-per-epoch state machine has no absorbing post-claim state during epoch zero.
proof: With `getEpoch() == 0`, initial `lastEpoch == 0`, finalized rewards, treasury approval, and `treasuryAllocationPerEpoch = 34_255e18`, call 1 sets `epochsSince=1`, pulls `34_255e18`, and writes `lastEpoch=0`. Call 2 sees the identical state and succeeds again; no caller restriction exists. Repeating 73 times queues `72 * 34_255e18 = 2_466_360e18` plus `33_640e18` on the last call, reaches `MAX_REWARDS = 2_500_000e18`, and returns only the last `615e18` excess to treasury. Thus a single epoch consumes the full lifetime cap.
description: During epoch zero, the permissionless once-per-epoch claim can be replayed until the full retention program cap is distributed.
fix: Track first-claim completion with a separate boolean (or store `epoch + 1`) and require a strict epoch advance after the first successful call.

LEAD | contract: LiquidationHandler | function: migrateCollateral | bug_class: pull-only-migration-dead-end | group_key: LiquidationHandler | migrateCollateral | pull-only-migration-dead-end
seam: execution×first-principles
trace: registry switches to replacement handler → retired handler can no longer call `processCollateral` because it is inactive → retired handler permits `migrateCollateral` only when `msg.sender` is exactly the replacement → the in-scope replacement implementation exposes no function that calls the retired handler or records its returned debt
code_smells: `migrateCollateral` is a pull endpoint restricted to `currentHandler`, but LiquidationHandler has no migration-acceptance entry point; the only test succeeds by `vm.prank(address(newLiquidationHandler))`, an address impersonation unavailable on-chain, and it does not assert that returned debt is booked in the replacement.
description: Standard replacement with another in-scope LiquidationHandler strands old collateral/debt unless governance deploys a different future handler or bespoke helper that can initiate and account for the pull.

LEAD | contract: GovStaker | function: migrateStake | bug_class: unchecked-permanent-flag-handoff | group_key: GovStaker | migrateStake | unchecked-permanent-flag-handoff
seam: execution×first-principles
trace: old staker proves the caller is permanent → moves the full stake to registry-selected staker → calls `onPermaStakeMigrate` → returns without checking the new staker's permanent flag → the in-scope base implementation of the hook is empty
code_smells: The irreversible property is delegated to an unauthenticated virtual hook with no postcondition; migrating into an unmodified GovStaker leaves `accountData[account].isPermaStaker == false`, after which the account can realize stake, cooldown, and unstake. Tests avoid this by deploying `MockGovStaker`, whose override authenticates the previous staker and sets the flag, so exploitability depends on the concrete future migration target.
description: A migration target that inherits or deploys the provided no-op hook can silently turn irrevocably permanent stake back into ordinary withdrawable stake.

VERIFICATION | `forge test --match-path test/integration/Retention.t.sol --match-test test_balanceChange -vv` could not compile because this clean audit worktree lacks LayerZero, BytesLib, and SSTORE2 imports; source-level traces and the existing integration assertions were used for proof.
